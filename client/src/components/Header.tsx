import Navigation from './Navigation';
import React from 'react';

const Header: React.FC = () => {
    return (
        <header>
            <Navigation />
        </header>
    )
}

export default Header;